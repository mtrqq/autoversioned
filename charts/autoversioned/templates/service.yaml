apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoversioned.fullname" . }}
  labels:
    {{- include "autoversioned.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autoversioned.selectorLabels" . | nindent 4 }}
